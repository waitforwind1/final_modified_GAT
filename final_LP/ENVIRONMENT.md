# Environment

## Expected Layout

This folder is self-contained for code, but it still expects the project-level dataset files to exist under:

- `data/facebook_ego/`
- `data/`

## Python Packages

The local reproduction in this workspace used:

- `python`
- `numpy`
- `scipy`
- `scikit-learn`
- `torch`
- `torch-geometric`

## Practical Notes

- GPU was used for the saved runs.
- The saved result files were produced on this machine from this workspace.
- Small numeric drift is still possible if library versions or CUDA behavior differ.
